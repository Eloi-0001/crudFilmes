spring:
  application:
    name: crudFilmes
  datasource:
    url: jdbc:h2:mem:filmes-test
    username: apiMovieTest
    password: test
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /banco-filme