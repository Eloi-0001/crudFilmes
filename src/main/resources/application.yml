spring:
  application:
    name: crudFilmes
  datasource:
    url: jdbc:h2:mem:filmes
    username: sa
    password: passoword
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /banco-filme